# Irrigation config profile — edit before flashing (valid JSON).
# Reflash after changes; not synced from Home Assistant.

nedorachio:
  config_profile: |
    {
      "version": 2,
      "global": {
        "watering_window": {
          "start": "23:00",
          "end": "09:00",
          "timezone": "America/New_York"
        },
        "rain_credit_mm_per_step": 10,
        "rain_credit_gallons_per_zone_per_step": 100,
        "rain_sensor_hold_hours_after_wet": 24,
        "attempt_cooldown_minutes": 20,
        "max_attempt_minutes": 30,
        "no_flow_grace_seconds": 60,
        "no_flow_sustain_seconds": 30,
        "blackout": { "weekdays": ["thu", "fri"] }
      },
      "zones": {
        "1": { "enabled": true, "weekly_goal_gallons": 400, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "2": { "enabled": true, "weekly_goal_gallons": 400, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "3": { "enabled": true, "weekly_goal_gallons": 400, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "4": { "enabled": true, "weekly_goal_gallons": 400, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "5": { "enabled": false, "weekly_goal_gallons": 100, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "6": { "enabled": false, "weekly_goal_gallons": 100, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "7": { "enabled": false, "weekly_goal_gallons": 100, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 },
        "8": { "enabled": false, "weekly_goal_gallons": 100, "start_minimum_psi": 35, "start_maximum_psi": 85, "minimum_running_psi": 20, "minimum_running_psi_grace_seconds": 60, "minimum_flow_gpm": 0.2, "maximum_flow_gpm": 12.0 }
      }
    }
